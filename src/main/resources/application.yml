spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/coupang
    username: root
    password: password123
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  elasticsearch:
    uris: http://localhost:9200


logging:
  level:
    org.elasticsearch.client: TRACE